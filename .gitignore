# Собирается через make-installer.sh, в репозитории не нужен:
# это base64 остальных файлов, каждый пересбор давал бы огромный диф.
# Для раздачи — прикладывать к GitHub Release.
jiggle-installer.sh

.DS_Store
*.iconset/
*.icns

# dmg для раздачи через Releases, в репозитории не нужен
*.dmg
